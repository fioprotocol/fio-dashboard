import React from 'react';
import ReplayIcon from '@mui/icons-material/Replay';

import classes from './WrapIcon.module.scss';

export const WrapIcon: React.FC = () => {
  return <ReplayIcon className={classes.icon} />;
};
