import React from 'react';

import RefreshIcon from '@mui/icons-material/Refresh';

import classes from './UnwrapIcon.module.scss';

export const UnwrapIcon: React.FC = () => {
  return <RefreshIcon className={classes.icon} />;
};
